import actionTypes from '../constant';
import initialState from '../reducers/initialState';

export default function smm(state = initialState, action) {
  switch(action.type) {
    default:
      return state;
  }
}